const r=require("express").Router(),U=require("../models/User"),b=require("bcryptjs"),j=require("jsonwebtoken");const t=u=>j.sign({id:u._id,name:u.name},process.env.JWT_SECRET,{expiresIn:"2d"});r.post("/register",async(q,s)=>{let{name,email,password}=q.body;if(await U.findOne({email}))return s.status(409).json({message:"Email exists"});let u=await U.create({name,email,password:await b.hash(password,10),bio:"Student • sharing ideas and projects",avatar:"https://i.pravatar.cc/150?img="+Math.ceil(Math.random()*70)});s.json({token:t(u),name:u.name})});r.post("/login",async(q,s)=>{let u=await U.findOne({email:q.body.email});if(!u||!(await b.compare(q.body.password||"",u.password)))return s.status(401).json({message:"Invalid login"});s.json({token:t(u),name:u.name})});module.exports=r;